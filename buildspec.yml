version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.12
    commands:
      - echo Installing dependencies...
      - pip install -r requirements.txt -t .

  build:
    commands:
      - echo Building the Lambda function zip...
      - zip -r function.zip .

artifacts:
  files:
    - function.zip